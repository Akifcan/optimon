{
  "_format": "hh3-sol-build-info-1",
  "id": "solc-0_8_28-40ce192aafd90b258954d2ccd0e7204aa4549daa",
  "solcVersion": "0.8.28",
  "solcLongVersion": "0.8.28+commit.7893614a",
  "userSourceNameMap": {
    "contracts/BaseStrategy.sol": "project/contracts/BaseStrategy.sol",
    "contracts/Counter.sol": "project/contracts/Counter.sol",
    "contracts/IStrategy.sol": "project/contracts/IStrategy.sol",
    "contracts/LendingStrategy.sol": "project/contracts/LendingStrategy.sol",
    "contracts/LPStrategy.sol": "project/contracts/LPStrategy.sol",
    "contracts/StakingStrategy.sol": "project/contracts/StakingStrategy.sol",
    "contracts/Vault.sol": "project/contracts/Vault.sol"
  },
  "input": {
    "language": "Solidity",
    "settings": {
      "evmVersion": "cancun",
      "outputSelection": {
        "*": {
          "": [
            "ast"
          ],
          "*": [
            "abi",
            "evm.bytecode",
            "evm.deployedBytecode",
            "evm.methodIdentifiers",
            "metadata"
          ]
        }
      },
      "remappings": []
    },
    "sources": {
      "project/contracts/BaseStrategy.sol": {
        "content": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.28;\n\nimport \"./IStrategy.sol\";\n\n/// @title BaseStrategy\n/// @notice Mock strategy that simulates yield accrual based on blocks elapsed.\n///         Each strategy has a different APY and risk score.\nabstract contract BaseStrategy is IStrategy {\n    address public immutable vault;\n    uint256 public principal;\n    uint256 public lastHarvestBlock;\n\n    modifier onlyVault() {\n        require(msg.sender == vault, \"only vault\");\n        _;\n    }\n\n    constructor(address _vault) {\n        vault = _vault;\n        lastHarvestBlock = block.number;\n    }\n\n    function deposit() external payable override onlyVault {\n        principal += msg.value;\n    }\n\n    function withdraw(uint256 amount) external override onlyVault {\n        require(amount <= address(this).balance, \"insufficient balance\");\n        principal = principal > amount ? principal - amount : 0;\n        (bool ok, ) = vault.call{value: amount}(\"\");\n        require(ok, \"transfer failed\");\n    }\n\n    function harvest() external override onlyVault {\n        uint256 yield_ = _pendingYield();\n        lastHarvestBlock = block.number;\n        if (yield_ > 0) {\n            // Mint simulated yield by keeping it in the contract balance\n            // In a real protocol this would come from the underlying protocol\n            principal += yield_;\n        }\n    }\n\n    function totalBalance() external view override returns (uint256) {\n        return principal + _pendingYield();\n    }\n\n    /// @dev Override this in each strategy to set the APY\n    function _apyBps() internal pure virtual returns (uint256);\n\n    function apyBps() external pure override returns (uint256) {\n        return _apyBps();\n    }\n\n    function _pendingYield() internal view returns (uint256) {\n        if (principal == 0) return 0;\n        uint256 blocksDelta = block.number - lastHarvestBlock;\n        // Simulate ~1 block per second, ~31.5M blocks per year\n        // yield = principal * apyBps / 10000 * blocksDelta / blocksPerYear\n        return (principal * _apyBps() * blocksDelta) / (10000 * 31_536_000);\n    }\n}\n"
      },
      "project/contracts/Counter.sol": {
        "content": "// SPDX-License-Identifier: UNLICENSED\npragma solidity ^0.8.28;\n\ncontract Counter {\n  uint public x;\n\n  event Increment(uint by);\n\n  function inc() public {\n    x++;\n    emit Increment(1);\n  }\n\n  function incBy(uint by) public {\n    require(by > 0, \"incBy: increment should be positive\");\n    x += by;\n    emit Increment(by);\n  }\n}\n"
      },
      "project/contracts/IStrategy.sol": {
        "content": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.28;\n\ninterface IStrategy {\n    /// @notice Deposit funds into the strategy\n    function deposit() external payable;\n\n    /// @notice Withdraw funds from the strategy\n    /// @param amount The amount to withdraw\n    function withdraw(uint256 amount) external;\n\n    /// @notice Harvest accrued yield and send it to the vault\n    function harvest() external;\n\n    /// @notice Total balance held by this strategy (principal + yield)\n    function totalBalance() external view returns (uint256);\n\n    /// @notice Current APY in basis points (e.g. 800 = 8%)\n    function apyBps() external view returns (uint256);\n\n    /// @notice Risk score 1-10\n    function riskScore() external view returns (uint256);\n}\n"
      },
      "project/contracts/LendingStrategy.sol": {
        "content": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.28;\n\nimport \"./BaseStrategy.sol\";\n\n/// @title LendingStrategy\n/// @notice Simulates a lending protocol — 8% APY, risk 7/10\ncontract LendingStrategy is BaseStrategy {\n    constructor(address _vault) BaseStrategy(_vault) {}\n\n    function _apyBps() internal pure override returns (uint256) {\n        return 800; // 8%\n    }\n\n    function riskScore() external pure override returns (uint256) {\n        return 7;\n    }\n\n    receive() external payable {}\n}\n"
      },
      "project/contracts/LPStrategy.sol": {
        "content": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.28;\n\nimport \"./BaseStrategy.sol\";\n\n/// @title LPStrategy\n/// @notice Simulates a liquidity pool — 14% APY, risk 9/10\ncontract LPStrategy is BaseStrategy {\n    constructor(address _vault) BaseStrategy(_vault) {}\n\n    function _apyBps() internal pure override returns (uint256) {\n        return 1400; // 14%\n    }\n\n    function riskScore() external pure override returns (uint256) {\n        return 9;\n    }\n\n    receive() external payable {}\n}\n"
      },
      "project/contracts/StakingStrategy.sol": {
        "content": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.28;\n\nimport \"./BaseStrategy.sol\";\n\n/// @title StakingStrategy\n/// @notice Simulates a staking protocol — 11% APY, risk 8/10\ncontract StakingStrategy is BaseStrategy {\n    constructor(address _vault) BaseStrategy(_vault) {}\n\n    function _apyBps() internal pure override returns (uint256) {\n        return 1100; // 11%\n    }\n\n    function riskScore() external pure override returns (uint256) {\n        return 8;\n    }\n\n    receive() external payable {}\n}\n"
      },
      "project/contracts/Vault.sol": {
        "content": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.28;\n\nimport \"./IStrategy.sol\";\n\n/// @title Vault\n/// @notice Core vault contract. Users deposit ETH, the vault distributes\n///         across strategies and tracks user shares.\ncontract Vault {\n    // --- Types ---\n    struct StrategyAllocation {\n        IStrategy strategy;\n        uint256 weight; // basis points, must sum to 10000\n    }\n\n    // --- State ---\n    address public owner;\n    StrategyAllocation[] public strategies;\n\n    uint256 public totalShares;\n    mapping(address => uint256) public shares;\n\n    // --- Events ---\n    event Deposit(address indexed user, uint256 amount, uint256 sharesMinted);\n    event Withdraw(address indexed user, uint256 amount, uint256 sharesBurned);\n    event Rebalance(uint256 timestamp);\n\n    // --- Modifiers ---\n    modifier onlyOwner() {\n        require(msg.sender == owner, \"only owner\");\n        _;\n    }\n\n    constructor() {\n        owner = msg.sender;\n    }\n\n    // --- Admin ---\n\n    /// @notice Add a strategy with a weight (in basis points)\n    function addStrategy(address _strategy, uint256 _weight) external onlyOwner {\n        strategies.push(StrategyAllocation({\n            strategy: IStrategy(_strategy),\n            weight: _weight\n        }));\n    }\n\n    /// @notice Update weights for all strategies. Must sum to 10000.\n    function setWeights(uint256[] calldata _weights) external onlyOwner {\n        require(_weights.length == strategies.length, \"length mismatch\");\n        uint256 total;\n        for (uint256 i = 0; i < _weights.length; i++) {\n            strategies[i].weight = _weights[i];\n            total += _weights[i];\n        }\n        require(total == 10000, \"weights must sum to 10000\");\n    }\n\n    // --- User Actions ---\n\n    /// @notice Deposit ETH into the vault\n    function deposit() external payable {\n        require(msg.value > 0, \"zero deposit\");\n        require(strategies.length > 0, \"no strategies\");\n\n        uint256 totalBefore = totalValue();\n        uint256 sharesToMint;\n\n        if (totalShares == 0) {\n            sharesToMint = msg.value;\n        } else {\n            sharesToMint = (msg.value * totalShares) / totalBefore;\n        }\n\n        shares[msg.sender] += sharesToMint;\n        totalShares += sharesToMint;\n\n        // Distribute to strategies by weight\n        uint256 remaining = msg.value;\n        for (uint256 i = 0; i < strategies.length; i++) {\n            uint256 amount;\n            if (i == strategies.length - 1) {\n                amount = remaining;\n            } else {\n                amount = (msg.value * strategies[i].weight) / 10000;\n                remaining -= amount;\n            }\n            if (amount > 0) {\n                strategies[i].strategy.deposit{value: amount}();\n            }\n        }\n\n        emit Deposit(msg.sender, msg.value, sharesToMint);\n    }\n\n    /// @notice Withdraw all shares\n    function withdraw() external {\n        uint256 userShares = shares[msg.sender];\n        require(userShares > 0, \"no shares\");\n\n        uint256 total = totalValue();\n        uint256 userAmount = (total * userShares) / totalShares;\n\n        shares[msg.sender] = 0;\n        totalShares -= userShares;\n\n        // Withdraw proportionally from each strategy\n        uint256 remaining = userAmount;\n        for (uint256 i = 0; i < strategies.length; i++) {\n            uint256 stratBal = strategies[i].strategy.totalBalance();\n            uint256 amount;\n            if (i == strategies.length - 1) {\n                amount = remaining > stratBal ? stratBal : remaining;\n            } else {\n                amount = (userAmount * stratBal) / total;\n                amount = amount > stratBal ? stratBal : amount;\n                remaining -= amount;\n            }\n            if (amount > 0) {\n                strategies[i].strategy.withdraw(amount);\n            }\n        }\n\n        (bool ok, ) = msg.sender.call{value: address(this).balance}(\"\");\n        require(ok, \"transfer failed\");\n\n        emit Withdraw(msg.sender, userAmount, userShares);\n    }\n\n    // --- Vault Operations ---\n\n    /// @notice Harvest yield from all strategies and rebalance funds\n    function rebalance() external onlyOwner {\n        // 1. Harvest all\n        for (uint256 i = 0; i < strategies.length; i++) {\n            strategies[i].strategy.harvest();\n        }\n\n        // 2. Pull all funds back\n        uint256 total;\n        for (uint256 i = 0; i < strategies.length; i++) {\n            uint256 bal = strategies[i].strategy.totalBalance();\n            if (bal > 0) {\n                strategies[i].strategy.withdraw(bal);\n            }\n            total += bal;\n        }\n\n        // 3. Redistribute by current weights\n        uint256 remaining = address(this).balance;\n        for (uint256 i = 0; i < strategies.length; i++) {\n            uint256 amount;\n            if (i == strategies.length - 1) {\n                amount = remaining;\n            } else {\n                amount = (total * strategies[i].weight) / 10000;\n                amount = amount > remaining ? remaining : amount;\n                remaining -= amount;\n            }\n            if (amount > 0) {\n                strategies[i].strategy.deposit{value: amount}();\n            }\n        }\n\n        emit Rebalance(block.timestamp);\n    }\n\n    // --- View ---\n\n    /// @notice Total value across all strategies\n    function totalValue() public view returns (uint256) {\n        uint256 total = address(this).balance;\n        for (uint256 i = 0; i < strategies.length; i++) {\n            total += strategies[i].strategy.totalBalance();\n        }\n        return total;\n    }\n\n    /// @notice Value of a user's shares\n    function userValue(address _user) external view returns (uint256) {\n        if (totalShares == 0) return 0;\n        return (totalValue() * shares[_user]) / totalShares;\n    }\n\n    /// @notice Number of strategies\n    function strategyCount() external view returns (uint256) {\n        return strategies.length;\n    }\n\n    receive() external payable {}\n}\n"
      }
    }
  }
}